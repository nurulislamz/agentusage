global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'agentusage'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['127.0.0.1:8080']
        labels:
          app: 'agentusage'
