# agentUsage Deep Space

Brand contract for `agentusage serve`. Canonical copy also lives in
`~/open-design/design-systems/agentusage/DESIGN.md` for OpenDesign Studio.

See that file for the full palette, type, component, and anti-slop rules.

**Direction:** dense operational TUI-native (JetBrains Mono, Deep Space
`#0C0E16` / accent `#7EB8F7`). Same information as the Bubble Tea split
view; CSS gauges and cards instead of unicode boxes.

**OpenDesign skills:** `frontend-design`, `impeccable-design-polish`.
**Craft:** `typography`, `color`, `anti-ai-slop`.
